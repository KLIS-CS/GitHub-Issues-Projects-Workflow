name: "Submit CP3"
description: "Submit your public CP3 practice repository for automatic and teacher grading."
title: "[CP3] Issues & Projects Submission"
body:
  - type: markdown
    attributes:
      value: |
        Submit the **repository URL** for your completed CP3 copy. Keep the copied repository Public so the KLIS-CS mother grader can read the trusted CP3 automatic grader output.

  - type: input
    id: repo
    attributes:
      label: Practice Repository URL
      description: Paste the repository URL. Nested GitHub URLs such as /tree/main are accepted.
      placeholder: https://github.com/YOUR-USERNAME/cp3-issues-projects-workflow
    validations:
      required: true

  - type: input
    id: username
    attributes:
      label: GitHub Username
      placeholder: YOUR-GITHUB-USERNAME
    validations:
      required: true

  - type: checkboxes
    id: integrity
    attributes:
      label: Integrity Check
      options:
        - label: I used the official KLIS-CS CP3 Copy Exercise.
          required: true
        - label: My CP3 branch, Issue, and open Pull Request are visible in this repository.
          required: true
        - label: I did not include passwords, API keys, tokens, or other secrets.
          required: true
